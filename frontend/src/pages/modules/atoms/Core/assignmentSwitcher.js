// 강의나 과제를 스위치하는 컴포넌트

import assignmentSelector from "./assignmentSelector";

function assignmentSwitcher(TARGET_OBJECT) {

    SWITCH TO TARGET_OBJECT;
    then,
    assignmentSelector(TARGET_OBJECT);

}

export default assignmentSwitcher;