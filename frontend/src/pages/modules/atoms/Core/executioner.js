/*        - 코드를 에디터 섹션에서 받아서 백엔드로 보내는 함수
       - 결과를 터미널 섹션과 테스트 케이스 섹션으로 전달한다 */

/*        - 실행 결과가 에러일 경우 => 코드 에디터 섹션에 대한 접근 필요
         - 에러 메시지에 따른 위치 확인
         - 에러 라인 하이라이트
         - 에러 메시지 제공 */

function executioner() {
    SEND_CODE_TO_BACKEND();
}

export default executioner;
